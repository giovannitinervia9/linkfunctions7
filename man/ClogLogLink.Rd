% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloglog_link.R
\name{ClogLogLink}
\alias{ClogLogLink}
\title{S7 Class for the Complementary Log-Log Link}
\usage{
ClogLogLink(
  link_name = character(0),
  link_bounds = integer(0),
  link_params = NULL
)
}
\arguments{
\item{link_name}{A character string naming the link, set by the
constructor and shown by \code{print()}.}

\item{link_bounds}{A length-two numeric vector, the open interval the
parameter lives in. Set by the constructor; see Value for this link's.}

\item{link_params}{A list of the link's own parameters, empty where it has
none. Set by the constructor.}
}
\value{
An S7 object of class \code{ClogLogLink}, inheriting from \code{\link[=link]{link()}} and
carrying its three properties \code{link_name}, \code{link_bounds} and
\code{link_params}. Its \code{link_bounds} are \code{c(0, 1)} and it carries no link parameters.
}
\description{
Carries the complementary log-log transformation
\eqn{\eta = \log(-\log(1-\theta))} on \eqn{(0, 1)}, with inverse
\eqn{\theta = 1 - \exp(-e^{\eta})}.

Unlike the logit and the probit it is asymmetric about \eqn{\theta = 1/2},
approaching one faster than zero, so it is the link of a
proportional-hazards model for a binary outcome.
}
\seealso{
\code{\link[=cloglog_link]{cloglog_link()}}, the constructor users call.
}
\keyword{internal}
