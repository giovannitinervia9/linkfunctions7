% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/softplus_link.R
\name{SoftplusLink}
\alias{SoftplusLink}
\alias{linkfun.SoftplusLink}
\alias{linkinv.SoftplusLink}
\alias{dlinkfun.SoftplusLink}
\alias{d2linkfun.SoftplusLink}
\alias{d3linkfun.SoftplusLink}
\alias{d4linkfun.SoftplusLink}
\alias{dlinkinv.SoftplusLink}
\alias{d2linkinv.SoftplusLink}
\alias{d3linkinv.SoftplusLink}
\alias{d4linkinv.SoftplusLink}
\title{S7 Class for the Softplus Link}
\usage{
SoftplusLink(
  link_name = character(0),
  link_bounds = integer(0),
  link_params = NULL,
  a = integer(0)
)
}
\arguments{
\item{link_name}{A character string naming the link, set by the
constructor and shown by \code{print()}.}

\item{link_bounds}{A length-two numeric vector, the open interval the
parameter lives in. Set by the constructor; see Value for this link's.}

\item{link_params}{A list of the link's own parameters, empty where it has
none. Set by the constructor.}

\item{a}{The scale parameter, strictly positive.}
}
\value{
An S7 object of class \code{SoftplusLink}, inheriting from \code{\link[=link]{link()}} and
carrying its three properties \code{link_name}, \code{link_bounds} and
\code{link_params}. Its \code{link_bounds} are \code{c(0, Inf)} and its \code{link_params} holds \code{a}.
}
\description{
Carries the softplus transformation on \eqn{(0, \infty)}, whose inverse
\eqn{\theta = \log(1 + e^{a\eta})/a} is a smooth approximation of
\eqn{\max(0, \eta)} that sharpens as the scale \eqn{a} grows.

It is the alternative to the log link for a positive parameter: the log link
maps a large negative \eqn{\eta} to something indistinguishable from zero,
while the softplus approaches zero linearly and stays numerically alive
there. The scale is stored in \code{link_params}, so one class serves every
\eqn{a}.
}
\section{Methods}{

Ten methods are registered on this class: \code{\link[=linkfun]{linkfun()}} and \code{\link[=linkinv]{linkinv()}},
and the four derivative orders in each direction, \code{\link[=dlinkfun]{dlinkfun()}} through
\code{\link[=d4linkfun]{d4linkfun()}} going out and \code{\link[=dlinkinv]{dlinkinv()}} through \code{\link[=d4linkinv]{d4linkinv()}} coming
back. The softplus is an antiderivative of the logistic, so its
inverse-link derivatives are the logistic ones shifted a place,
\eqn{h^{(k+1)} = a^k \sigma^{(k)}}, and they reuse the same polynomials
the logit does. The forward set comes from a compiled kernel written in
\eqn{u = -\mathrm{expm1}(-a\theta)}, which is finite where the
\eqn{\mathrm{expm1}(a\theta)} form overflows.
}

\seealso{
\code{\link[=softplus_link]{softplus_link()}}, the constructor users call.
}
\keyword{internal}
